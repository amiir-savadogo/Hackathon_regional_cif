# Archive - prototype V1

Ces fichiers correspondent au tout premier prototype (XGBoost sur 4 variables,
`../data/credit_wa_dataset.csv`, dataset qui n'existe plus dans ce dépôt). Ils
ne sont plus utilisés par `ai-service/main.py`, qui charge désormais les
artefacts depuis `ai-service/models/` (produits par `scripts/02_train_model.py`
à la racine du dépôt, sur `data/samde_dataset.csv`, 30 variables).

Conservés à titre d'historique uniquement — ne pas exécuter tel quel.
